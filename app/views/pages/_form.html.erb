
<%= simple_form_for :user, url: pages_support_path do |f| %>
  <%= f.input :full_name, label: "Full Name:" %>
  <%= f.input :email %>
  <%= f.input :message %>
  <%= f.button :submit, "Send" %>
<% end %>