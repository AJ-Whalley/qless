<h1>hello</h1>
  <% if flash[:notice] %>
    <div class="notice"><%= flash[:notice] %></div>
  <% end %>

<p><strong>Jobs Approved:</strong></p> 
<% current_user.applications.each do |a| %>
 <% if a.approved == true %>
 <% j = @jobs.find(a.job_id) %>
  <div>
   <%= image_tag j.user.profile.image_url(:job_card)%><br>
   <%= j.user.profile.first_name %>
   <%= j.user.profile.last_name %>
  </div><br>

    <div>
   <%= j.title %> <br>
   <ul>
    <li><%= j.street_address %></li>
    <li><%= j.city %></li>
    <li><%= j.started_at %></li>
    <li>$<%= j.price %></li>
    <li><%= j.instructions %></li>
   </ul>
   <%= link_to 'See Job', job_path(j.id) %>




  </div>
  <% else %>
  <% end %>
<% end %>

<p><strong>Jobs Applied For:</strong></p> 
<% current_user.applications.each do |a| %>
<% if a.approved == nil  %>
<% j = @jobs.find(a.job_id) %>
  <div>
   <%= image_tag j.user.profile.image_url(:job_card)%><br>
   <%= j.user.profile.first_name %>
   <%= j.user.profile.last_name %>
  </div><br>

    <div>
   <%= j.title %> <br>
   <ul>
    <li><%= j.street_address %></li>
    <li><%= j.city %></li>
    <li><%= j.started_at %></li>
    <li>$<%= j.price %></li>
    <li><%= j.instructions %></li>
   </ul>
   <%= link_to 'See Job', job_path(j.id) %>
  </div>
  <% else %>
<% end %>
<% end %>

 <%= link_to 'Back', pages_user_home_path %> |
 <%= link_to 'Find New Job', pages_user_home_path %>